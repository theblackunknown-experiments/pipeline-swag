#ifndef BPIPE_PARAMETER_TYPES_H
#define BPIPE_PARAMETER_TYPES_H

#include "bpipe/config.hpp"

namespace bpipe {
	namespace types {

enum ParameterType {
	Scalar,
	Texture,
	Text,
};

	}  // namespace types

} // namespace bpipe

#endif
